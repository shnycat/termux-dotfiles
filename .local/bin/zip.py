#!/usr/bin/env python
from pyzipper import AESZipFile as Az
import sys

f = sys.argv[1]
w = sys.argv[2]

o = Az(f)
print(i)

raise
with open(w, 'rb') as ws:
  for i in ws:
    try:
      o.pwd = w.strip()
      o.read('Reverse-Engineering-with-Radare-2/download lab.txt')
    except:
      pass
    else:
      print(word.decode().strip())
      exit(0)

